DA FINIRE

    - Scrivere test sulle macro

    - Finire il replace del pool di vector a pool di array: fare test performance nell'engine
        - Finire cachine
            - Introdurre nel caching anche gli ataacks di opposite

    - Completare test unita'

REFACTORING

    - La chiamata all'engine dovrebbe essere fatta senza passare le mosse
        - Se le calcola all'interno e di conseguenza anche il caso di fine partita.

GENERALE

    - Ottimizzare uso della cache
        - Per esempio in perft 6 viene usata il 73% delle volte ma
            - la chiave della mappa fa schifo (usare long)
            - vedere se la mappa e' quella giusta
            - introdurne l'uso anche nei motori
                - in questo caso va mantenuta smilza (cancellare le mosse non piu' possibili)

    - Introdurre logging (usare spdlog) oppure introdurre il format nel log casalingo che abbiamo
i
    - Rivedere tutti i passi per arrivare a calcolare le mosse possibili (Perft)
        - Probabilmente faccio delle cose in piu' e/o piu' volte (per esempio il calcolo della numerosita' dei pezzi)\
            - generateLegalMoves
                - generatePseudoLegalMoves
                    - getDestinationPositions
                - isValid
                    - simulateMove
                    - checkControl
                        - getAttacks
                            - getPawnAttacks
                            - getKnightAttacks
                            - ...
                    - undoSimulateMove



VALUTAZIONE

    - Il valore del materiale è la parte più influente della valutazione.

    - Dato il valore dei singoli pezzi (intero invece che double) occorre anche dare un valore a:
        - pedoni raddoppiati
        - pedoni bloccati
        - pedoni isolati

    - Utilizzare le tabelle che danno punteggio bonus a seconda della posizione
        - Queste tabelle possono variare a seconda della fase di gioco: apertura, medio, finale
            - Stabilire il momento di cambio della fase
                - A questo proposito ho implementato il calcolo della fase a seconda dei pezzi rimasti e la utilizzo per pesare
                la valutazione tra medio gioco e gioco finale

SEARCH

    - Iniziato ab_engine con motore che fa search alpha-beta

